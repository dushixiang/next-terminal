import React from 'react';
import Info from "../Info";

const MyInfo = () => {

    return (
        <div style={{backgroundColor: "white"}}>
            <Info></Info>
        </div>
    );
};

export default MyInfo;